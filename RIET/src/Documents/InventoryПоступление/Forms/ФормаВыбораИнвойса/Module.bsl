
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Инвойсы.Параметры.УстановитьЗначениеПараметра("ConversionFactor", Параметры.UOM.ConversionFactor);
	Инвойсы.Параметры.УстановитьЗначениеПараметра("Код", СокрЛП(Параметры.Good.Код));
	Инвойсы.Параметры.УстановитьЗначениеПараметра("PO", Параметры.PO);
	Инвойсы.Параметры.УстановитьЗначениеПараметра("UOM", Параметры.UOM);
	
КонецПроцедуры

&НаКлиенте
Процедура Таблица1Выбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	ОповеститьОВыборе(Элемент.ДанныеСтроки(ВыбраннаяСтрока).Цена);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьИнвойс(Команда)
	
	Данные = ЭтаФорма.Элементы.Таблица1.ТекущиеДанные;
	Если Не Данные = Неопределено Тогда
		ОткрытьФорму("Документ.Инвойс.ФормаОбъекта", Новый Структура("Ключ", Данные.Инвойс));
	КонецЕсли;
	
КонецПроцедуры
