
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	Если ПодразделениеОрганизацииЗаполнено(ПараметрКоманды) Тогда 
		УправлениеПечатьюКлиент.ВыполнитьКомандуПечати("Документ.ПередачаОС", "ОС1", ПараметрКоманды,
														 ПараметрыВыполненияКоманды);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция ПодразделениеОрганизацииЗаполнено(Док)
	
	Если Не ЗначениеЗаполнено(Док.ПодразделениеОрганизации) Тогда
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Необходимо заполнить поле ""Налоговая регистрация"" на вкладке ""Дополнительно""!");
		Возврат Ложь;
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции
	
