
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
Если Параметры.Свойство("СтруктураНастройки") Тогда
		
		СтруктураНастройки = Параметры.СтруктураНастройки;
		Если СтруктураНастройки.Имя = "ВыборИзОбработкиПерепроведениеСКПДляМА" Тогда
			НастроитьДляВыбораИзОбработкиПерепроведениеСКПДляМА();
		КонецЕсли;
			 		
	КонецЕсли;
			 		
КонецПроцедуры

&НаСервере
Процедура НастроитьДляВыбораИзОбработкиПерепроведениеСКПДляМА()
	
	СписокВалютДляИсключения = Новый СписокЗначений;
	СписокВалютДляИсключения.Добавить(Справочники.Валюты.НайтиПоКоду("643"));
	СписокВалютДляИсключения.Добавить(Справочники.Валюты.НайтиПоНаименованию("USD"));
											   
	ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбора(Список.Отбор, "ВалютаДокумента", 
											   СписокВалютДляИсключения,
											   ВидСравненияКомпоновкиДанных.НеВСписке);
										   
КонецПроцедуры


