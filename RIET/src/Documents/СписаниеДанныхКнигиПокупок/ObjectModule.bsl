
Процедура ОбработкаПроведения(Отказ, Режим)
	//{{__КОНСТРУКТОР_ДВИЖЕНИЙ_РЕГИСТРОВ
	// This fragment was built by the wizard.
	// Warning! All manually made changes will be lost next time you use the wizard.
	Для Каждого ТекСтрокаДанные Из Данные Цикл
		// register ДанныеКнигиПокупок Расход
		Движение = Движения.ДанныеКнигиПокупок.Добавить();
		Движение.ВидДвижения = ВидДвиженияНакопления.Расход;
		Движение.Период = Дата;
		Движение.Организация = Организация;
		Движение.Документ = ТекСтрокаДанные.Документ;
		Движение.ОписаниеЗатрат = ТекСтрокаДанные.ОписаниеЗатрат;
		Движение.Поставщик = ТекСтрокаДанные.Поставщик;
		Движение.НомерПрихода = ТекСтрокаДанные.НомерПрихода;
		Движение.ДатаПрихода = ТекСтрокаДанные.ДатаПрихода;
		Движение.ПлатежныйЦентр = ТекСтрокаДанные.ПлатежныйЦентр;
		Движение.ПО = ТекСтрокаДанные.ПО;
		Движение.НомерСчета = ТекСтрокаДанные.НомерСчета;
		Движение.НомерВаучера = ТекСтрокаДанные.НомерВаучера;
		Движение.Валюта = ТекСтрокаДанные.Валюта;
		Движение.Сумма = ТекСтрокаДанные.Сумма;
	КонецЦикла;
	// writing register movements
	Движения.ДанныеКнигиПокупок.Записать();
	//}}__КОНСТРУКТОР_ДВИЖЕНИЙ_РЕГИСТРОВ
КонецПроцедуры
