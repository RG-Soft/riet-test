
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Отбор = Параметры.Отбор;
	
	Отбор.Вставить("Отменен", Ложь);
	
	ImportExportСервер.ДобавитьОтборПоProcessLevel(Отбор, "ProcessLevel");
	
	// { RGS AArsentev 07.06.2018
	Если CustomsСервер.ЭтоБрокер() Тогда
		Элементы.NumberOfLines.Видимость = Истина;
		Элементы.NumberOfUniqueLines.Видимость = Истина;
	КонецЕсли;
	// } RGS AArsentev 07.06.2018
	
КонецПроцедуры