
&НаСервере
Процедура ЗаполнитьДляСпискаНаСервере()
	Если Объект.ОС.Количество() > 1 Тогда
		ДокументСтроки1 = Объект.ОС[0].ДокументОС;	
		Для каждого Строка Из Объект.ОС  Цикл
			Строка.ДокументОС = ДокументСтроки1;
		КонецЦикла;
	КонецЕсли; 
КонецПроцедуры

&НаКлиенте
Процедура ЗаполнитьДляСписка(Команда)
	ЗаполнитьДляСпискаНаСервере();
КонецПроцедуры

&НаКлиенте
Процедура ОСДокументОСНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	ТекущиеДанные = Элементы.ОС.ТекущиеДанные;
	ТекЗначение = ТекущиеДанные.ДокументОС;
	
	Если ЗначениеЗаполнено(ТекЗначение) Тогда
		Элемент.ВыбиратьТип = Ложь;
	КонецЕсли; 
	
КонецПроцедуры
