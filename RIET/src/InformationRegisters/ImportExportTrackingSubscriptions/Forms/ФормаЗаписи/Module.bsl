
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ПолноценнаяЗапись = РеквизитФормыВЗначение("Запись");
	Если НЕ ПолноценнаяЗапись.Выбран() Тогда
		Запись.User = ПараметрыСеанса.ТекущийПользователь;
	КонецЕсли;
	
КонецПроцедуры
