
Функция ДатаЗавершенияОбработкиКоординатором(Реестр) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	СтатусыРеестровLog.Date как ДатаСтатуса
	|ИЗ
	|	РегистрСведений.СтатусыРеестровLog КАК СтатусыРеестровLog
	|ГДЕ
	|	СтатусыРеестровLog.LogTo = &Реестр
	|	И СтатусыРеестровLog.LogType = ЗНАЧЕНИЕ(Справочник.LogTypes.ИзменениеСтатуса)
	|
	|УПОРЯДОЧИТЬ ПО
	|	СтатусыРеестровLog.Date";
	
	Запрос.УстановитьПараметр("Реестр", Реестр);
	
	Результат = Запрос.Выполнить().Выбрать();
	
	Если Результат.Следующий() Тогда
		Возврат Результат.ДатаСтатуса;
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции