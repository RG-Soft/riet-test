
Процедура ПередЗаписью(Отказ, Замещение)
	
	Для Каждого Запись Из ЭтотОбъект Цикл	
		Запись.ПериодДанных = НачалоМесяца(Запись.ПериодДанных);
	КонецЦикла;
	
	Если ЭтотОбъект.Количество() Тогда
		Отбор.ПериодДанных.Значение = ЭтотОбъект[0].ПериодДанных;
	КонецЕсли;
	
КонецПроцедуры
