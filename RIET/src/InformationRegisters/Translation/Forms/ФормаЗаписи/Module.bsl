
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Этаформа.Параметры.ЗначенияЗаполнения.Свойство("Label") Тогда
		Лейбл = Этаформа.Параметры.ЗначенияЗаполнения.Label;
		Запись.Label = Лейбл;
	КонецЕсли; 
	
	Если Запись.Language = "" Тогда
		Запись.Language = "US";
	КонецЕсли; 
	
КонецПроцедуры

&НаКлиенте
Процедура LanguageНачалоВыбораИзСписка(Элемент, СтандартнаяОбработка)
	
	СписокЯзыков = ПовтИсп.ПолучитьСписокЯзыков();
	Элемент.СписокВыбора.Очистить();	
	Для каждого ЭлСписка Из СписокЯзыков Цикл
		Элемент.СписокВыбора.Добавить(ЭлСписка.Значение, ЭлСписка.Представление);
	КонецЦикла;	
	
КонецПроцедуры
