
&НаКлиенте
Процедура СписокПередУдалением(Элемент, Отказ)
	
	// Изменине реквизитов и статусов удалять нельзя
	// То, что запись удаляет именно тот пользователь, кто ее создал, контролируется в RLS
	LogType = Элемент.ТекущиеДанные.LogType;
	Если ЗначениеЗаполнено(LogType)
		И (LogType = ПредопределенноеЗначение("Справочник.LogTypes.ИзменениеРеквизитов")
		ИЛИ LogType = ПредопределенноеЗначение("Справочник.LogTypes.ИзменениеСтатуса")) Тогда
		Отказ = Истина;
	КонецЕсли;
	
КонецПроцедуры
