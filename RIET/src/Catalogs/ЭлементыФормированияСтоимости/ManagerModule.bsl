
Функция ПолучитьПоPaymentKind(PaymentKind) Экспорт
	
	// возращает элемент формирования стоимости по Payment kind
	// payment kind должен быть заполнен
	// для неизвестных payment kinds возвращается неопределено
	
	Если НЕ ЗначениеЗаполнено(PaymentKind) Тогда
		ВызватьИсключение "Payment kind is empty!";
	КонецЕсли;
	
	ПервыйСимволPaymentKind = Лев(PaymentKind, 1);
	Если ПервыйСимволPaymentKind = "1" или PaymentKind = "9120" или PaymentKind = "9150" Тогда
		Возврат ТаможняСборы;
	ИначеЕсли ПервыйСимволPaymentKind = "2" или ПервыйСимволPaymentKind = "3" Тогда
		Возврат ТаможняПошлины;
	ИначеЕсли ПервыйСимволPaymentKind = "5" Тогда
		Возврат ТаможняНДС;
	Иначе
		Возврат Неопределено;
	КонецЕсли;
	
КонецФункции