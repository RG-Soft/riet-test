
&НаСервере
Процедура ПередЗаписьюНаСервере(Отказ, ТекущийОбъект, ПараметрыЗаписи)
	
	РГСофт.ЗаполнитьModification(ТекущийОбъект);
	
КонецПроцедуры

&НаКлиенте
Процедура КомандаПоказатьНаКартеНажатие(Команда)
	
	СтруктураПараметров = Новый Структура("Location", Объект.Ссылка);
	Оповестить("ПоказатьСкладНаКарте", СтруктураПараметров);
	ОткрытьФорму("Обработка.WorkplaceForOperator.Форма", СтруктураПараметров);
		
КонецПроцедуры
