
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Отбор = Параметры.Отбор;
	
	Если НЕ Отбор.Свойство("ПометкаУдаления") Тогда
		Отбор.Вставить("ПометкаУдаления", Ложь);
	КонецЕсли;
	
	Если Параметры.Свойство("Import") Тогда
		Отбор.Вставить("Import", Истина);
	КонецЕсли;
	
	Если Параметры.Свойство("Export") Тогда
		Отбор.Вставить("Export", Истина);
	КонецЕсли;
	
КонецПроцедуры
