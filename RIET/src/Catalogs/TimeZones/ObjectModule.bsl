
Процедура ПередЗаписью(Отказ)
	
	Код = СокрЛП(Код);
	
	Попытка
	
		StandardTimeOffset = СмещениеСтандартногоВремени(Код)/60/60;
	
	Исключение
		
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(
			"Failed to save 'Standard time offset, h', check the code of time-zone!",
			ЭтотОбъект, "Код", , Отказ);
			
	КонецПопытки;
		 	
КонецПроцедуры
