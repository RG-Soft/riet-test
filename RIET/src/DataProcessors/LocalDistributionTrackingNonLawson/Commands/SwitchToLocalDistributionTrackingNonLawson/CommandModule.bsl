
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ОкноДляВывода = Неопределено; 
	Окна = ПолучитьОкна();
	Для Каждого Окно Из Окна Цикл
		Если Окно.Основное = Истина Тогда
			ОкноДляВывода = Окно;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	ПараметрыФормы = Новый Структура("SwitchToLocalDistributionTrackingNonLawson");
	ОткрытьФорму("Обработка.LocalDistributionTrackingNonLawson.Форма", ПараметрыФормы,,,ОкноДляВывода);

КонецПроцедуры
