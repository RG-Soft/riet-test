
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ОкноДляВывода = Неопределено; 
	Окна = ПолучитьОкна();
	Для Каждого Окно Из Окна Цикл
		Если Окно.Основное = Истина Тогда
			ОкноДляВывода = Окно;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	ПараметрыФормы = Новый Структура("SwitchToImportExportTracking");
	ОткрытьФорму("Обработка.ImportExportTracking.Форма", ПараметрыФормы,,,ОкноДляВывода);

КонецПроцедуры
