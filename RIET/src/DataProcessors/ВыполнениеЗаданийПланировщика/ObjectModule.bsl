Процедура Запустить() Экспорт
	
	ЗагрузитьКурсы();	
	
КонецПроцедуры

Процедура ЗагрузитьКурсы()
	
	Обработка = Обработки.КурсыВалютРБК.Создать();
	Обработка.НачДата = НачалоМесяца(ТекущаяДата()) - 3*3600*24; // - 3 дня - на всякий случай
	Обработка.КонДата = КонецМесяца(ТекущаяДата());
	Выборка = Справочники.Валюты.Выбрать();
	Список = новый СписокЗначений;
	Пока Выборка.Следующий() Цикл
		СтрокаТЧ = Обработка.СписокВалют.Добавить();
		СтрокаТЧ.Валюта = Выборка.Ссылка;
	КонецЦикла;	
	
	Обработка.ЗагрузитьКурсыСРБК( Неопределено, Неопределено);
		
КонецПроцедуры
